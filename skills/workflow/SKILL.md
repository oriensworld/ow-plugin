---
name: workflow
description: Guide software work through Research, Design, Plan, Execute, Optimize, and Review stages. Use when the user requests a structured end-to-end development workflow with checkpoints and quality control.
---

# Development Workflow

Read [the shared six-stage workflow](../../commands/workflow.md) completely before proceeding.

Treat `$ARGUMENTS` as the task description and ignore Claude-only frontmatter. Keep the six named stages, but follow the active host's collaboration and approval model. Store task plans under `.claude/plan/` on Claude and `.codex/plan/` on Codex. Use available documentation tools rather than assuming a specific provider such as Context7.
